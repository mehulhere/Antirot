# ——— Backend API (required) ———
# Everything needed to run the standalone backend

ANTIROT_BACKEND_BIND=127.0.0.1:8787
DATABASE_URL=postgres://antirot_backend:change-me@localhost/antirot_backend
ANTIROT_ADMIN_TOKEN=change-me-admin-token
ANTIROT_DEVICE_TOKEN=change-me-device-token
GOOGLE_IOS_CLIENT_ID=973993815360-7q908kk99vtbvv07648prppfdbacqddr.apps.googleusercontent.com
ANTIROT_WORKSPACE_ID=main
ANTIROT_APNS_ENV=sandbox
ANTIROT_APNS_TEAM_ID=
ANTIROT_APNS_KEY_ID=
ANTIROT_APNS_PRIVATE_KEY_PATH=/etc/antirot/AuthKey_XXXXXXXXXX.p8
ANTIROT_APNS_TOPIC=com.mehulhere.Antirot
RUST_LOG=antirot_bridge=info,tower_http=info

# ——— Voice-first iOS coach input/output ———
# Fireworks Whisper v3 is the default speech-to-text model.
FIREWORKS_BASE_URL=https://api.fireworks.ai/inference/v1
FIREWORKS_AUDIO_BASE_URL=https://audio-prod.api.fireworks.ai/v1
FIREWORKS_API_KEY=
FIREWORKS_STT_MODEL=whisper-v3

# Async Flash v1.5 is streaming/WebSocket-only, so the backend proxies streaming TTS.
ASYNC_BASE_URL=https://api.async.com
ASYNC_API_KEY=
ASYNC_TTS_API_KEY=
ASYNC_TTS_MODEL=async_flash_v1.5
ASYNC_TTS_VOICE_ID=

# ——— Semantic memory search (Gemini primary, Voyage fallback) ———
# Antirot intentionally does not auto-detect embedding providers.
# Keep Gemini as primary; use Voyage only as fallback.
ANTIROT_MEMORY_EMBEDDING_MODEL=gemini-embedding-001
ANTIROT_MEMORY_EMBEDDING_FALLBACK_MODEL=voyage-4-large
ANTIROT_MEMORY_GEMINI_API_KEY=
ANTIROT_MEMORY_VOYAGE_API_KEY=

# ——— LLM regression judge (optional, for paid-product quality gates) ———
# Use a separate judge model to score live transcript quality after `test:backend-userflows-llm`.
CROF_BASE_URL=https://crof.ai/v2
CROF_API_KEY=
ANTIROT_JUDGE_MODEL=qwen3.5-9b
ANTIROT_JUDGE_EFFORT_LEVEL=none
ANTIROT_JUDGE_MIN_OVERALL=8
ANTIROT_JUDGE_MIN_DIMENSION=7

# ——— OpenClaw Plugin (optional, for self-hosted setups) ———

ANTIROT_WORKSPACE_DIR=
ANTIROT_BRIDGE_URL=https://api.antirot.org
ANTIROT_BRIDGE_DEVICE_ID=
# normalAlarmCommand="paplay /path/to/normal-alarm.wav"
# alarmCommand="paplay /path/to/loud-alarm.wav"
