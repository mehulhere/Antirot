# ——— Backend API (required) ———
# Everything needed to run the standalone backend

ANTIROT_BACKEND_BIND=127.0.0.1:8787
DATABASE_URL=postgres://antirot_backend:change-me@localhost/antirot_backend
ANTIROT_ADMIN_TOKEN=change-me-admin-token
ANTIROT_DEVICE_TOKEN=change-me-device-token
ANTIROT_JWT_SECRET=change-me-30-day-session-secret
# 64 hex characters (32 random bytes) used only to encrypt BYOK credentials at rest.
ANTIROT_BYOK_ENCRYPTION_KEY_HEX=
# Never expose ANTIROT_ADMIN_TOKEN, ANTIROT_DEVICE_TOKEN, or ANTIROT_JWT_SECRET through NEXT_PUBLIC_* variables or static browser files.
# Keep disabled in production unless anonymous browser accounts are an intentional product choice.
ANTIROT_ALLOW_ANONYMOUS_SESSIONS=0
# Enable only during a controlled legacy Android/manual device enrollment window.
ANTIROT_ALLOW_LEGACY_DEVICE_BOOTSTRAP=0
# Comma-separated browser origins allowed to call the backend. Native apps do not use CORS.
ANTIROT_CORS_ALLOWED_ORIGINS=https://antirot.org,https://www.antirot.org,http://localhost:3000,http://127.0.0.1:3000,http://localhost:3001,http://127.0.0.1:3001
GOOGLE_IOS_CLIENT_ID=your-google-ios-client-id.apps.googleusercontent.com
# your-google-ios-client-id means the OAuth client identifier created for the iOS app in Google Cloud Console.
GOOGLE_ANDROID_CLIENT_ID=
# GOOGLE_ANDROID_CLIENT_ID means the OAuth client identifier bound to the Android package and signing certificate.
GOOGLE_WEB_CLIENT_ID=
NEXT_PUBLIC_GOOGLE_WEB_CLIENT_ID=
ANTIROT_WORKSPACE_ID=main
ANTIROT_APNS_ENV=production
ANTIROT_APNS_TEAM_ID=
ANTIROT_APNS_KEY_ID=
ANTIROT_APNS_PRIVATE_KEY_PATH=/etc/antirot/AuthKey_XXXXXXXXXX.p8
ANTIROT_APNS_TOPIC=com.mehulhere.Antirot
RUST_LOG=antirot_backend=info,tower_http=info

# ——— Provider safety budgets ———
ANTIROT_CHAT_CONCURRENCY_LIMIT=12
ANTIROT_SPEECH_CONCURRENCY_LIMIT=4
ANTIROT_SPEECH_DAILY_STT_BYTES=104857600
ANTIROT_SPEECH_DAILY_TTS_CHARS=50000
ANTIROT_MEMORY_DAILY_EMBEDDING_CALLS=500

# ——— Coach LLM (Vertex-only for tailored/default users) ———
# Paste the full Google service-account JSON content on one line.
GOOGLE_CLOUD_CREDENTIALS=

# ——— Voice-first iOS coach input/output ———
# Smallest Pulse HTTP STT receives VAD-sized audio clips from the frontend/backend.
SMALLEST_STT_URL=https://api.smallest.ai/waves/v1/stt/
SMALLEST_API_KEY=

# Inworld streaming TTS returns audio chunks that the backend proxies to apps.
INWORLD_BASE_URL=https://api.inworld.ai
INWORLD_API_KEY=
INWORLD_TTS_MODEL=inworld-tts-1.5-mini
INWORLD_TTS_VOICE_ID=Dennis

# ——— Semantic memory search (Gemini primary, Voyage fallback) ———
# Antirot intentionally does not auto-detect embedding providers.
# Keep Gemini as primary; use Voyage only as fallback.
# Canonical writes do not call providers. The background memory index worker drains durable jobs every 5 seconds.
ANTIROT_MEMORY_EMBEDDING_MODEL=gemini-embedding-001
ANTIROT_MEMORY_EMBEDDING_FALLBACK_MODEL=voyage-4-large
ANTIROT_MEMORY_GEMINI_API_KEY=
ANTIROT_MEMORY_VOYAGE_API_KEY=

# ——— LLM regression judge (optional, for paid-product quality gates) ———
# Use a separate judge model to score live transcript quality after `test:backend-userflows-llm`.
CROF_BASE_URL=https://crof.ai/v2
CROF_API_KEY=
ANTIROT_JUDGE_MODEL=qwen3.5-9b
ANTIROT_JUDGE_EFFORT_LEVEL=none
ANTIROT_JUDGE_MIN_OVERALL=8
ANTIROT_JUDGE_MIN_DIMENSION=7
